<ul id="content-pages-dashboard">
  <% Page.pages_with_content.each do |page| %>
    <li><%= link_to page.title, page_path(page), :target => "_blank" %> [<%= link_to "Edit", edit_admin_page_path(page) %>]</li>
  <% end %>
</ul>
